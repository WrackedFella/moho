# engine_core

Core engine types and utilities used by Moho. This README is intentionally short — conceptual background and constraints have been moved to `docs/engine_core/CONCEPTS.md`.

Quick start
-----------
- See `docs/engine_core/CONCEPTS.md` for architecture notes and shader/CPU data layout requirements.

Tests
-----
- Unit tests are under `engine_core/tests/` (e.g. `instance_pod.rs`) and validate POD/layout invariants used by the renderer.

