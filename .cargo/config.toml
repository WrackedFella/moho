# Pass ALSA library path to the linker.
# rust-lld (default since Rust 1.86) doesn't find -lasound in the Flatpak sandbox
# without an explicit search path.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-L/usr/lib/x86_64-linux-gnu"]
